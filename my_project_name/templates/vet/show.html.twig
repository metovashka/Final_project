{% extends 'base.html.twig' %}

{% block title %}Vet{% endblock %}

{% block body %}
    <h1>Vet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vet.id }}</td>
            </tr>
            <tr>
                <th>Specialty</th>
                <td>{{ vet.Specialty }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vet.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ vet.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vet_index') }}">back to list</a>

    <a href="{{ path('vet_edit', {'id': vet.id}) }}">edit</a>

    {{ include('vet/_delete_form.html.twig') }}
{% endblock %}
